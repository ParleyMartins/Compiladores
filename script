#!/bin/bash

EXEC=./prog
INPUT=./entrada
OUTPUT=./saida.py

make 

$EXEC <$INPUT >$OUTPUT

for line in $(cat $OUTPUT); do
	break;
done

if [ $line = "Error:" ]; then
	cat $OUTPUT
else
	echo "Compilacao realizada com sucesso"
fi
